API_URL=https://your-api-host.com/api

# College portal (React app) — used for LRP / lead report WebView (no /api suffix)
WEB_APP_URL=https://focalyt.com

# Google OAuth (web-type client, same as frontend uses)
# Get from Google Cloud Console > OAuth credentials.
GOOGLE_OAUTH_CLIENT_ID=449914901350-ibgtfl0tbog7vb91u7d5s9cmo92ba1kg.apps.googleusercontent.com
# Web app origin (LRP / lead report pages in-app WebView). Same host as college portal.
GOOGLE_OAUTH_REDIRECT_URI=https://focalyt.com
