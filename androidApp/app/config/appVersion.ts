/**
 * Must match android/app/build.gradle on each release:
 *   versionCode  → APP_VERSION_CODE
 *   versionName  → APP_VERSION_NAME
 */
export const APP_VERSION_CODE = 1;
export const APP_VERSION_NAME = '1.0';
